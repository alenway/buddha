export default function Footer() {
  return (
    <footer className="border-t-2 border-ink px-4 md:px-16 py-6 flex justify-between items-center flex-wrap gap-4">
      <p className="font-mono text-[11px] text-ink-3 tracking-[.08em]">
        © The Visual Math Series — Foreshortening
      </p>
      <p className="font-mono text-[11px] text-ink-3 tracking-[.08em]">
        Visible = Real × cos(θ) or sin(θ)
      </p>
    </footer>
  );
}
